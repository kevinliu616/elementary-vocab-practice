# 國中小必備英文單字練習 — 使用說明

本應用是**純本機運行的網頁程式**，收錄 2005 個台灣國中小常用必背單字，提供五種練習模式，幫助你熟悉並背熟這些單字。

---

## 一、如何啟動與停止

### 啟動（二選一）

**方式 A：雙擊啟動檔（推薦）**

1. 打開資料夾 `/Volumes/home/Doubao/國中小英文單字練習/`
2. 雙擊 **`啟動單字練習.command`**
3. 會自動啟動伺服器，並用 Chrome 開啟練習頁面

**方式 B：手動開啟**

1. 打開 Chrome 瀏覽器
2. 網址列輸入：`http://localhost:8766`

### 停止

雙擊 **`停止單字練習.command`** 即可停止背景伺服器。

> 提醒：練習完直接關掉瀏覽器分頁也可以，伺服器在背景運行幾乎不佔資源。

---

## 二、環境需求

| 項目 | 需求 |
|------|------|
| 瀏覽器 | **Chrome** 或 **Edge**（Safari 不支援語音辨識） |
| 網址 | 必須用 `http://localhost:8766` 開啟 |
| 麥克風 | 跟讀練習需要，第一次使用請按「允許」 |
| 作業系統 | macOS / Windows 皆可 |

> **注意**：不要用 `file://` 直接打開 `index.html`，那會讓麥克風和語音功能被瀏覽器停用。

---

## 三、首頁說明

首頁顯示三大資訊：

- **總單字數**：2005 個
- **已熟練**：你標記為「會了」的單字數
- **待加強**：還沒記熟的單字數

下方有學習進度條，進度會**自動存在瀏覽器本機**（localStorage），下次開啟會接續進度。

### 選擇練習範圍

首頁下方可以選擇練習範圍：

- **全部**：2005 個單字全部練習
- **A～Z 字母分類**：只練習特定字母開頭的單字

---

## 四、五種練習模式

### 1. 單字卡翻轉（📇）

- 看英文單字，想它的中文意思
- **點卡片**翻面看答案
- 按「🔊」可聽發音
- 按「我會了」標記熟練、按「還不會」繼續加強

### 2. 中選英測驗（📝）

- 看中文意思，從四個選項選出正確的英文單字
- 答完自動顯示正確答案與得分
- 每輪最多 20 題

### 3. 英選中測驗（🎯）

- 看英文單字，從四個選項選出正確的中文意思
- **滑鼠移到選項上會自動發音**，方便聽音辨字
- 每輪最多 20 題

### 4. 拼字練習（⌨️）

- 看中文意思，在輸入框打出正確的英文拼字
- 輸入框上方會顯示**字母數提示**（例：apple → ▢ ▢ ▢ ▢ ▢），邊輸入邊填上
- 按「🔊 聽發音」可聽發音提示
- 按 Enter 或「確認答案」送出，答錯會顯示正確答案

### 5. 跟讀發音（🗣️）

- 顯示英文單字和中文意思
- 按「🔊 聽標準發音」聽正確唸法
- 按「🎤 開始跟讀」開啟麥克風，大聲唸出來
- 系統會**辨識你的發音**，唸對了自動跳下一題
- 也可以用「我會讀了」「還不熟練」手動標記

---

## 五、發音功能說明

- 所有模式都內建英文發音（Web Speech API）
- 發音會自動選擇系統中品質最好的英文語音
- 發音速度刻意放慢（0.75 倍速），適合學習
- 第一次點發音若沒聲音，重新整理頁面再試

---

## 六、進度儲存

- 進度儲存在**目前使用的瀏覽器**本機
- 標記「我會了」的單字會累計到「已熟練」
- 換瀏覽器或清除瀏覽器資料，進度會重來

---

## 七、常見問題（FAQ）

**Q1：跟讀練習顯示「不支援語音辨識」？**
請確認：① 用 Chrome 或 Edge；② 網址是 `http://localhost:8766`（不是 `file://`）；③ 已允許麥克風權限。

**Q2：麥克風權限被拒絕了？**
點瀏覽器網址列左邊的鎖頭圖示，把麥克風改為「允許」，再重新整理頁面。

**Q3：發音聽起來很奇怪？**
重新整理頁面讓語音列表完整載入，系統會自動選用較好的語音。

**Q4：進度不見了？**
進度存在瀏覽器本機，請用同一個瀏覽器開啟。若清除過瀏覽器資料，進度會歸零。

**Q5：可以離線使用嗎？**
可以。啟動檔會在本機啟動伺服器，不需要網路連線。

---

## 八、專案檔案結構

```
/Volumes/home/Doubao/國中小英文單字練習/
├── index.html              # 主頁面（唯一入口）
├── style.css               # 樣式
├── app.js                  # 程式邏輯
├── words-data.js           # 2005 個單字資料
├── 啟動單字練習.command    # 雙擊啟動
└── 停止單字練習.command    # 雙擊停止
```

---

*最後更新：2026-09-24*
